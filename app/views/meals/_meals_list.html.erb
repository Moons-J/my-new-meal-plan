<div data-search-element-target="list">
  <div>
    <% if @query.present? %>
      <p>Searched for <strong><%= @query %></strong>, found <%= @meals.count %> meals</p>
    <% else %>
      <p>All <%= @meals.count %> meals</p>
    <% end %>
  </div>
  <div class="meal-cards-container" data-controller="swipe-delete">
    <% @meals.each do |meal| %>
      <%= render "meal_card", meal: meal %>
    <% end %>
  </div>
</div>
